<div class="wrapper">
  <%= render "shared/header" %>

  <div class="mein">

    <div class="nav-area">
    <nav class="nav">
      <a class="nav-link active" aria-current="page" href="#">アカウント</a>
      <a class="nav-link" href="#">アカウント編集</a>
      <a class="nav-link" href="#">登録情報</a>
    </nav>
    </div>

    <%= @user.nickname %>
    <%= @user.email %>
    <%= @user.age.name %>
    <%= @user.category.name %>





  </div>
</div>